<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.servicesaroundme">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ServicesAroundMe">
        <activity
            android:name=".CoreCapabilities.baseActivity"
            android:label="@string/title_activity_base"></activity>

        <uses-library
            android:name="com.google.android.app.wearable"
            android:required="false" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Testing.testMapsActivity"
            android:label="@string/title_activity_test_maps" />
        <activity android:name=".Testing.testActivity" />
        <activity android:name=".MyAccount.userAccountActivity" />
        <activity android:name=".OrderPurchases.ordersActivity" />
        <activity android:name=".ServiceCatalogue.serviceCatalogueActivity" />
        <activity android:name=".QuotesMgmt.quotesActivity" />
        <activity android:name=".PreferencesUserMgmt.preferencesActivity" />
        <activity android:name=".Inbox.inboxActivity" />
        <activity android:name=".Help.helpActivity" />
        <activity android:name=".Feedback.feedbackActivity" />
        <activity android:name=".Favourites.favouritesActivity" />
        <activity android:name=".CoreCapabilities.homeActivity" />
        <activity android:name=".MyAccount.loginRegisterationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>